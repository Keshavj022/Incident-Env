apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments
  namespace: incident-env
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payments
  template:
    metadata:
      labels:
        app: payments
    spec:
      containers:
        - name: payments
          image: incident-env:latest
          env:
            - name: DEPLOYMENT_MODE
              value: distributed
            - name: SERVICE_NAME
              value: payments
            - name: DATABASE_URL
              value: postgresql://incident:$(POSTGRES_PASSWORD)@postgres:5432/incident_env
            - name: REDIS_URL
              value: redis://redis:6379
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: payments
  namespace: incident-env
spec:
  selector:
    app: payments
  ports:
    - port: 8000
